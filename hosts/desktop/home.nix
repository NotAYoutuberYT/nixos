{ ... }:

{
  specialConfig = {
    hyprland.sensitivity = "-0.65";
    hyprland.hw-cursor-cpu-buffer = true;

    hyprland.workspaces = [
      "1, monitor:DP-5"
    ];

    bundles = {
      desktop.enable = true;
      command-line.enable = true;
    };
  };
}
